# VITE_API_BASE_URL é auto-detetado via window.location.hostname
# Para override (ex: produção), define a variável de ambiente antes de iniciar o Vite
# set VITE_API_BASE_URL=http://192.168.1.100:8000 && npx vite
